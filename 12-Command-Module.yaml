- name: Learning command module
  hosts: local
  connection: local
  tasks:
  - name: This is to check if the command module works
    ansible.builtin.command: cat /etc/passwd 
    register: user_data

  - name: Print user data
    ansible.builtin.debug: 
      msg: "{{ user_data }}"