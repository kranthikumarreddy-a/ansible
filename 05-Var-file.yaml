- name: Loading the Variable from the another file
  hosts: local
  connection: local
  vars_files:
  - Varfile.yaml
  tasks:
  - name: Load variables from file
    vars:
      NAME: Shiva
    ansible.builtin.debug:
      msg: "User data is: {{ NAME }}, {{Number}}, {{Village}}"    